<% flash.each do |type, message| %>
  <div class="ui eight column centered grid">
    <div class="ui <%= alert_classes[type] %> message">
      <div class="header">
        <%= message %>
      </div>
      <i class="close icon"></i>
    </div>
  </div>
<% end %>
